<div class="container">

    <!-- GOOGLE INFORMATION --->
        <div class="col-sm-6">
            <div class="well">
                <h3 class="text-danger"><span class="fa fa-google-plus"></span> Signed with Google</h3>

                <!--p>
                    <strong>id</strong>: {{this.usuario.google.id}} <br>
                    <strong>token</strong>: {{this.google.token}} <br>
                    <strong>email</strong>: {{this.google.email}} <br>
                    <strong>name</strong>: {{this.usuario.google.name}} 
                </p--->

            </div>
        </div-->

    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="header">Mi contenido
            </h1>
        </div>
    </div>
    <!-- /.row -->
    <table class="table">
        <tr>
            <th>Titulo</th>
            <th>Privado</th>
            <th>Imagen</th>
            <th>Acciones</th>
        </tr>
      {{#each videos}}
          <tr>
              <td>{{this.title}}</td>
              <td>{{this.privateFile}}</td>
              <td><a href="{{this.video}}" target="_blank" class="btn">Ver video</a></td>
              <td>
                  <a href="/editar/{{this.id}}" class="btn">Editar</a>
              </td>
          </tr>
      {{/each}}
    </table>


</div>

